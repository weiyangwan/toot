<div class="container col-md-offset-2 col-md-9">

  <div class="follow-header">
    <h5 class="follow-current"><%= @title %></h5>
    <% if @title == "following" %>
      <h5><%= link_to "followers", followers_user_path(@user), class: "show-follow-Btn" %></h5>
    <% else %>
      <h5><%= link_to "following", following_user_path(@user), class: "show-follow-Btn" %></h5>
    <% end %>
  </div>

  <% if @users.any? %>
    <ul class="users follow">
      <%= render @users %>
    </ul>
    <%= will_paginate %>
  <% end %>
</div>
